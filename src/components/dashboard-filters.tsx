import { Filter } from "lucide-react";
import type { AthleteListItem, GroupListItem } from "@/lib/data";

export function DashboardFilters({ athletes, groups, values }: { athletes: AthleteListItem[]; groups: GroupListItem[]; values: { scope: string; athlete?: string; group?: string; range: string; from?: string; to?: string } }) {
  const selectClass = "min-h-11 rounded-xl border border-[#d4dfe4] bg-white px-3 text-sm font-semibold text-[#304a5d] outline-none focus:border-[#16a5b8]";
  return <form method="get" className="surface-card flex flex-wrap items-end gap-3 p-4"><div className="mr-1 hidden size-10 place-items-center rounded-xl bg-[#e7f3f4] text-[#0a6f7e] sm:grid"><Filter className="size-4" /></div><label><span className="mb-1.5 block text-[.65rem] font-bold uppercase tracking-[.1em] text-[#82929d]">View</span><select name="scope" defaultValue={values.scope} className={selectClass}><option value="all">Whole team</option><option value="men">All men</option><option value="women">All women</option><option value="group">Specific group</option><option value="individual">Individual athlete</option></select></label>{values.scope === "individual" && <label><span className="mb-1.5 block text-[.65rem] font-bold uppercase tracking-[.1em] text-[#82929d]">Athlete</span><select name="athlete" defaultValue={values.athlete} className={`${selectClass} max-w-56`} required><option value="">Select athlete</option>{athletes.map((athlete) => <option key={athlete.id} value={athlete.id}>{athlete.displayName}</option>)}</select></label>}{values.scope === "group" && <label><span className="mb-1.5 block text-[.65rem] font-bold uppercase tracking-[.1em] text-[#82929d]">Group</span><select name="group" defaultValue={values.group} className={selectClass} required><option value="">Select group</option>{groups.map((group) => <option key={group.id} value={group.id}>{group.name}</option>)}</select></label>}<label><span className="mb-1.5 block text-[.65rem] font-bold uppercase tracking-[.1em] text-[#82929d]">Period</span><select name="range" defaultValue={values.range} className={selectClass}><option value="week">7 days</option><option value="month">30 days</option><option value="all">All</option><option value="custom">Custom</option></select></label>{values.range === "custom" && <><label><span className="mb-1.5 block text-[.65rem] font-bold uppercase tracking-[.1em] text-[#82929d]">From</span><input type="date" name="from" defaultValue={values.from} className={selectClass} required /></label><label><span className="mb-1.5 block text-[.65rem] font-bold uppercase tracking-[.1em] text-[#82929d]">To</span><input type="date" name="to" defaultValue={values.to} className={selectClass} required /></label></>}<button className="min-h-11 rounded-xl bg-[#0a304a] px-5 text-sm font-semibold text-white">Apply filters</button></form>;
}
